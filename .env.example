# Gesture to LLM - Environment Variables

# Webhook URL - The endpoint that receives the gesture text and returns LLM responses
# This should be a URL that accepts POST requests with JSON payload
WEBHOOK_URL=https://your-n8n-or-llm-webhook-endpoint.com/webhook

# Session ID - Optional identifier for maintaining conversation context
# Useful if your webhook endpoint supports session-based conversations
SESSION_ID=session_001

# Timeout in seconds - How long to wait before sending the buffered text
# After this many seconds of inactivity, the accumulated words will be sent
TIMEOUT_SECONDS=4.0

# Confidence threshold - Minimum confidence for gesture detection
# Values between 0.0 and 1.0, higher values require more confident detections
CONFIDENCE_THRESHOLD=0.7

# Gesture cooldown in seconds - Minimum time between identical gesture detections
# Helps prevent repeated triggering of the same gesture
GESTURE_COOLDOWN_SECONDS=2.5

# Notes:
# - WEBHOOK_URL is required and must be a valid URL that accepts JSON payloads
# - All other variables are optional and have default values if not set
# - Copy this file to .env and update the values for your specific setup